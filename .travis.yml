sudo: required

language: c

services:
  - docker

before_install:
  - docker pull micchon/baccaml

script:
  - docker run -u root -v $(pwd):/tmp --rm -it micchon/baccaml /home/opam/.opam/4.05.0/bin/jbuilder runtest
