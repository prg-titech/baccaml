(jbuild_version 1)

(ocamllex (lexer))
(ocamlyacc (parser))

; (menhir ((modules (parser))))

(library
 ((name           minCaml)
  (public_name    baccaml.minCaml)
  (modules        (:standard \ min_caml anchor))
  (libraries      (str core logs util))
  (c_names        (float))
  (preprocess
   (pps (ppx_deriving.show)))
))

(executable
 ((name           min_caml)
  (public_name    baccaml.min_caml)
  (modules        Min_caml)
  (libraries      (core minCaml))
))
