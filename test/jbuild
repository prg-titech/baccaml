(jbuild_version 1)

(executables
 ((names (simple1_test simple2_test simple3_test simple4_test))
  (libraries (core str ounit mincaml baccaml_jit))
 ))

(alias
 ((name runtest)
  (deps (simple1_test.exe))
  (action  (run ${<})
)))

(alias
 ((name runtest)
  (deps (simple2_test.exe))
  (action  (run ${<})
)))

(alias
 ((name runtest)
  (deps (simple3_test.exe))
  (action  (run ${<})
)))

(alias
 ((name runtest)
  (deps (simple4_test.exe))
  (action  (run ${<})
)))

(alias
 ((name simpletest)
  (deps (simple4_test.exe))
  (action  (run ${<})
)))


